# MRA RUO / ReasonRelation Phase 18

## Added

- `ReasonGraphBindingNode` in the Surface AST.
- Deterministic `ReasonGraphBindingIR` projection in the Reason IR.

## Compatibility

The addition is metadata-only during generic compilation; it performs no I/O
and does not alter generic Runtime execution.
