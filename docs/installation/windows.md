# Installing on Windows 11

From PowerShell, run `powershell -ExecutionPolicy Bypass -File scripts/install.ps1 -NonInteractive -Json`. The default root is `%LOCALAPPDATA%\ReasonScript`; add its `bin` directory to the user PATH if needed.
