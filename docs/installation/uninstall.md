# Uninstalling

Run `scripts/uninstall.sh --dry-run` (or `scripts/uninstall.ps1 -DryRun`) to inspect managed paths. Run without `--dry-run` to remove installed versions, the active link, CLI wrapper, and manifest. `--purge` also removes cache, installation artifacts, and user configuration. Projects are never removed.
