# Installing on Linux

Requires Python 3.11 or newer and Git. Run `./scripts/install.sh --non-interactive`; then add `~/.reasonscript/bin` to `PATH`. The installer is user-scoped and does not require root privileges or alter shell profiles.
