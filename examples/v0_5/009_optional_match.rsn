module OptionalMatch {
  fn Score(value: optional<int>) -> int {
    match value {
      some(x) => return x
      none => return 0
    }
  }

  calculation Answer {
    result = Score(some(42))
  }
}
