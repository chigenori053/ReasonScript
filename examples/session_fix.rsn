module SessionFix {
  fn PatchSessionMachine(gap: int) -> int {
    return gap + 1
  }

  calculation TransitionGap {
    result = 1
  }

  calculation StableState {
    result = PatchSessionMachine(TransitionGap)
  }
}
