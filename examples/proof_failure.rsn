// Expected to fail semantic validation: the state transition is inconsistent
// because CurrentState and ProposedState depend on each other cyclically,
// so no deterministic transition can be proven.
module ProofFailure {
  calculation CurrentState {
    result = ProposedState + 1
  }

  calculation ProposedState {
    result = CurrentState + 1
  }
}
