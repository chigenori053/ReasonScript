module Test {
    fn Score(score: int) -> int {
        match score {
            80..100 => return 1
            default => return 0
        }
    }

    calculation Result {
        result = Score(85)
    }
}
