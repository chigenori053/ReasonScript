module Basic {
    enum Color {
        Red
        Blue
        Green
    }

    fn Score(color: Color) -> int {
        match color {
            Color.Red => return 1
            default => return 0
        }
    }

    calculation Result {
        result = Score(Color.Green)
    }
}
