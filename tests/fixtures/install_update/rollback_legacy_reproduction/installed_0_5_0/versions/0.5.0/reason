#!/usr/bin/env python3
import json
from pathlib import Path

version = (Path(__file__).resolve().parent / "VERSION").read_text(encoding="utf-8").strip()
print(json.dumps({"status": "pass", "reason_version": version}))
