module Test {
    struct Point {
        x: int
        y: int
    }

    fn Score(point: Point) -> int {
        match point {
            Point { x } | Point { y } => return x
            Point { } => return 0
        }
    }
}
