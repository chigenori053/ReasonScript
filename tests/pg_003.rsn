module Test {
    struct Point {
        x: int
    }

    fn Score(point: Point) -> int {
        match point {
            Point { } when 123 => return 1
            Point { } => return 0
        }
    }
}
