# Unit Tests

Official unit validation is routed by `scripts/test_platform.py unit`.

The runner maps this category to the repository's existing parser, validator,
semantic, Reason IR, runtime, and calculation unit suites.
