module Test {
    fn Score(value: optional<int>) -> int {
        match value {
            some(x) => return x
            none => return 0
        }
    }

    calculation Result {
        result = Score(some(10))
    }
}
