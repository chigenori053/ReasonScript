module Test {
    struct Point {
        x: int
        y: int
    }

    fn Score(point: Point) -> int {
        match point {
            Point { x, y } => return x + y
            default => return 0
        }
    }

    calculation Result {
        result = Score(Point { x: 10, y: 20 })
    }
}
