# Integration Tests

Official integration validation is routed by `scripts/test_platform.py integration`.

The runner executes the complete source-to-knowledge pipeline suites, including
MSI, FSI, RI, KBE, SDK, toolchain, and IDE integration coverage.
