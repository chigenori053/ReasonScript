[package]
name = "reasonscript-computation-runtime-cli"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Phase 3 primitive execution CLI for reason-computation-ir/0.1."

[[bin]]
name = "reason-computation-runtime"
path = "src/main.rs"

[dependencies]
reasonscript-computation-ir = { path = "../computation-ir" }
serde_json = "1.0"
