[package]
name = "reasonscript-computation-ir"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Phase 3 skeleton: reason-computation-ir/0.1 decoder and a Tensor-less basic-block VM."

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
